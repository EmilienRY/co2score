import 'package:flutter/material.dart';


class pageVisu extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('page visu du plat'),
      ),
    );

  }
}